<%if line_item == @current_item%>
    <tr class='line-item-highlight'>
<% else%>
<tr>
<% end %>
<td><%= line_item.quantity %>&times;</td>
<td><%= line_item.product.title %></td>
<td class="item_price"><%= sprintf("₴%0.02f", line_item.total_price) %></td>
</tr>